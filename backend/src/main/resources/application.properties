server.port=8007
spring.application.name=cs-agent-system

# 跨域配置
cors.allowed-origins=http://localhost:3007

# 坐席配置
agent.max-capacity=3
agent.timeout-seconds=60
vip.max-jump-count=2

# 定时任务配置
scheduler.stats-interval-ms=10000
scheduler.recycle-interval-ms=30000
